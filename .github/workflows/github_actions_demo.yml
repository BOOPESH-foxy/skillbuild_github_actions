name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push:
    paths-ignore:
      - 'mac-app/**'
      - '!mac-app/*.txt'
env:  
  name: event-trigger
  timeout:  5

jobs:
  job1:
    name: print-trigger
    runs-on: ubuntu-latest
    steps:
      - name: process exit
        run: exit 1

      - name: if failed
        if: ${{ always () }}
        run: echo "prev step failed"


  # job2:
  #   name: print-job
  #   if: ${{ success() }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: echo "hi ${{github.actor}} this is another job"
