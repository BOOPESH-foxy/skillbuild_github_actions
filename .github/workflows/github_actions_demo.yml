name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: 
  push

jobs:
  dump-the-context:
    runs-on:  ubuntu-latest
    steps:  
      - name: Dump the context
        env:  
          JOB_CONTEXT: ${{ toJson(github) }}
        run:  echo  "$JOB_CONTEXT.repository_owner"