name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: 
  push

jobs:
  job1:
    steps:
      - name: set env
        id: step_one
        run:  | 
          echo "$NAME=BOO"  >> "$GITHUB_ENV"

      - name: use env
        id: step_two
        run:  |
          echo "$NAME"