name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push

jobs:
  job1:
    name: print-trigger
    runs-on: ubuntu-latest
    steps:
      - name: process exit
        run: echo "::add-mask::boo"
        run:  | 
          echo "boo"
          echo "boo"
          echo "boo"
          echo "boo"

  job2:
    name: print-job
    if: ${{ success() }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "::add-mask::github.actor" 
      - run: echo "hi ${{github.actor}} this is another job"
