name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push

env: 
  secret-val:112388 
  actor: ${{ github.actor }}
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - id: set-output-secret
        name: set-secret
        run: |
          secret=$secret-val
          echo "::add-mask::$secret"
          echo "secret-num=$secret" >> "$GITHUB_OUTPUT"
          
      - name: use-output
        run:  echo "${{ steps.set-output-secret.outputs.secret-num }}"

  # job2:
  #   name: print-job
  #   if: ${{ success() }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: | 
  #           echo "::add-mask::$actor" 
  #           echo "hi ${{github.actor}} this is another job"
