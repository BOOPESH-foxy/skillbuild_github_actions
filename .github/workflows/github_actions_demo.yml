on:
  push:
    branches:
      - main

jobs:
  semantic_versioning:
    name: publish zip
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: list directory
      run:  | 
        sudo apt-get install zip
        zip -r mac-app.zip mac-app
        ls