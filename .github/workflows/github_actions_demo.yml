name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: 
  push

jobs:
  job1:
    runs-on:  ubuntu-latest
    steps:
      - name: functions
        id: step_one
        if: ${{ startsWith('hello','oh') }}
        run:  |
          echo "hey boo"
